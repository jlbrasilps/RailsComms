<%= turbo_frame_tag :dashboard do %>
  <div class="w-full h-screen pt-8">
    <h1 class="text-2xl text-gray-100 text-center"> <%= @server.name %> invite link </h1>
    <div class="max-w-2xl w-full mx-auto rounded-lg bg-gray-500 text-lg text-gray-100 p-2"> 
      <%= root_url %>invite/<%= @invite_link.uniq_id %>
    </div>
  </div>
<% end %>